import { LoginDirectiveDirective } from './login-directive.directive';

describe('LoginDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
